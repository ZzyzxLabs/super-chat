# Server-side only. The proxy route injects this; it never reaches the browser.
OPENAI_API_KEY=sk-...

# Optional: point at an OpenAI-compatible endpoint (a router, vLLM, Ollama).
# OPENAI_BASE_URL=https://api.openai.com/v1
